{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Nest Debugger",
      "request": "launch",
      "command": "npm run start:dev --watch",
      "cwd": "${workspaceFolder}"
    }
  ]
}
